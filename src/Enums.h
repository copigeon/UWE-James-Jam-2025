#pragma once

enum GameState
{
	MENU,
	INTRO,
	PLAYING,
	TYPECHECK,
	GAME_WON,
	GAME_LOST,
	QUIT,
	PAUSE,
};

enum Audiotypes
{
	SFX,
	ENVIRO,
	NPC,
	PLAYER,
	NARRATIVE,
};

enum Menu_Graphics
{
	BACKGROUND,
	FOREGROUND,
};